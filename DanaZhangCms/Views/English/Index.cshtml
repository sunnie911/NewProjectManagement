@model DanaZhangCms.ViewModels.HomeVM
@{
    Layout = "~/Views/Shared/_LayoutEn.cshtml";
}
@section css{
    <link rel="stylesheet" type="text/css" href="/css/new/index.css" />
    <link rel="stylesheet" type="text/css" href="css/new/swiper.min.css" />
}
<div class="whole">
    <!-- banner -->
    <div class="banner">
        <div class="common_containe">
            <div class="swiper-container">
                <div class="swiper-wrapper">
                    @if (Model.Banners != null && Model.Banners.Any())
                    {
                        foreach (var item in Model.Banners)
                        {
                            <div class="swiper-slide">
                                <div class="slide-left">
                                    <div class="bt">@item.NameEn </div>
                                    <span>@item.SummaryEn</span>
                                    <a class="readMore" target="_blank" href="@item.LinkUrlEn">Read More</a>
                                </div>
                                <div class="slide-right">
                                    <img src="@item.ImgUrl">
                                </div>
                            </div>
                        }

                    }
                </div>
                <div class="swiper-pagination"></div>
            </div>
            <div class="swiper-button-next"></div>
            <div class="swiper-button-prev"></div>
        </div>

    </div>
    <!-- 热卖详情 -->
    <div class="hotSell common_containe">
        <div class="index_title">
            <div class="title">{{titleArry[0].english}}</div>
            <div class="main">
                <span>{{titleArry[0].english}}</span>
                <div class="more">
                    <a href="/Product" target="_blank">
                        <span>More</span>
                        <img src="images/to1.png">
                    </a>
                </div>

            </div>

        </div>
        <div class="content">
            <ul>
                @if (Model.Products != null && Model.Products.Any())
                {
                    foreach (var item in Model.Products)
                    {
                        <li>
                            <a href="@(Url.Action("ProDetail","english",new { id=item.Id}))" target="_blank">
                                <img class="goods_img" src="@item.ImgUrl">
                                <span>@item.NameEn</span>
                                @if (item.IsHot)
                                {
                                    <img class="hot" src="images/sale.png">
                                }

                            </a>
                        </li>
                    }
                }
            </ul>
        </div>
    </div>
    <!-- 简介 -->
    <div class="introduce_bj">
        <div class="introduce common_containe">
            <div class="in_left">
                <div class="index_title">
                    <div class="title">{{titleArry[1].english}}</div>
                    <div class="main">
                        <span>{{titleArry[1].english}}</span>
                    </div>
                </div>
                <p>
                    We are a leading enterprise introduced through the "5213 plan" for entrepreneurship and innovation of high-level talents in Xiaoshan District,
                    Hangzhou. We are a national high-tech enterprise specializing in the development, production and sales of concrete 3D printing and high-end testing instruments and equipment.
                    Our R & D and production bases are located in Xintang science and innovation Park, Xiaoshan District, Hangzhou and Jincheng City, Shanxi Province. 
                    "Continuous research and development, cost reduction and efficiency increase, and being the longest friend of users" is our vision.
                    We have established a research and development team composed of doctors, masters and well-known experts from Tsinghua University and Zhejiang University......
                </p>
                <div class="btn"><a href="/english/contact" target="_blank"><img src="images/to.png"></a></div>
            </div>
            <div class="in_right">
                <img class="leftop" src="images/leftop.png">
                <img src="images/about.png">
                <img class="rightdown" src="images/rightdown.png">
            </div>
        </div>
    </div>
    <!-- 精彩视频 -->
    <div class="vedio">
        <div class="common_containe">
            <div class="index_title">
                <div class="title">{{titleArry[2].english}}</div>
                <div class="main">
                    <span>{{titleArry[2].english}}</span>
                    <div class="more">
                        <a href="/english/vedio" target="_blank">
                            <span>More</span>
                            <img src="images/to1.png">
                        </a>
                    </div>
                </div>
            </div>


            <div class="vedio_container swiper-container">
                <div class="swiper-wrapper">
                    @if (Model.Vedios != null && Model.Vedios.Any())
                    {
                        foreach (var item in Model.Vedios)
                        {
                            <div class="swiper-slide">
                                <div class="slide_main">
                                    <a href="/english/VedioDetail/@item.Id">
                                        <img src="@item.ImgUrl">
                                        </a>
                                        <div class="mainP">@item.TitleEn</div>
                                        <span>@item.CreatedDate</span>
                                </div>
                            </div>
                        }
                    }
                </div>

            </div>

            <div class="vedio_more"><a href="/english/vedio" target="_blank">Read More</a></div>

        </div>
    </div>
    <!-- 学术交流 -->
    <div class="learning common_containe">
        <div class="index_title">
            <div class="title">{{titleArry[3].english}}</div>
            <div class="main">
                <span>{{titleArry[3].english}}</span>
                <div class="more">
                    <a href="/english/article" target="_blank">
                        <span>More</span>
                        <img src="images/to1.png">
                    </a>
                </div>
            </div>
        </div>
        <ul class="learning-box">
            @if (Model.Articles != null && Model.Articles.Any())
            {
                foreach (var item in Model.Articles)
                {
                    <li>
                        <img src="@item.ImgUrl">
                        <div class="main">
                            <div class="title">  <a href="@(Url.Action("newDetail","english",new { id=item.Id}))"> @item.Title </a></div>
                            <div class="neirong">
                            </div>
                        </div>
                        <div class="mengceng"></div>
                    </li>
                }
            }


        </ul>
    </div>
    <!-- 合作机构 -->
    <div class="cooperative common_containe">
        <div class="index_title">
            <div class="title">{{titleArry[4].english}}</div>
            <div class="main">
                @*<span>{{titleArry[4].english}}</span>*@
                <div class="more">
                    <a href="list/learning.html" target="_blank">
                        <span>More</span>
                        <img src="images/to1.png">
                    </a>
                </div>
            </div>
        </div>
        <ul>
            @*<li v-for="item in cooperativeArry"><img :src="item"></li>*@
            @if (Model.Logos != null && Model.Logos.Any())
            {
                foreach (var item in Model.Logos)
                {
                    <li><img src="@item.ImgUrl" title="@item.Title"></li>
                }
            }
        </ul>
    </div>

</div>


@section Scripts{
    <script src="/js/new/swiper.min.js" type="text/javascript"></script>
    <script src="/js/new/index.js" type="text/javascript"></script>
}
